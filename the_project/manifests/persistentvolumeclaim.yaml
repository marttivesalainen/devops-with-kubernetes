apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: shared-claim
spec:
  storageClassName: shared-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi